# Vietnamese Legal RAG — Chunking + Retrieval Pipeline
# Core dependencies

chromadb>=0.5.0
sentence-transformers>=3.0.0
transformers>=4.40.0
pyyaml>=6.0
numpy>=1.24.0

# Optional: progress bars during embedding
tqdm>=4.66.0
